name: Smoke

on:
  workflow_call:
    inputs:
      base_url:
        required: true
        type: string

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/smoke-test
        with:
          base-url: ${{ inputs.base_url }}
