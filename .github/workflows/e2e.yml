name: E2E

on:
  workflow_call:
    inputs:
      base_url:
        required: true
        type: string

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/e2e-test
        with:
          base-url: ${{ inputs.base_url }}
