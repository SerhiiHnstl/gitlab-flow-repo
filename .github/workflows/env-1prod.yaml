name: ENV · 1prod

on:
  push:
    branches: ['1prod']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate direct prod change
        run: echo "🔒 [1prod] Direct change on prod branch detected. Running validation + redeploy."

  ci:
    needs: validate
    uses: ./.github/workflows/ci.yml
    with:
      environment: '1prod'

  deploy:
    needs: ci
    uses: ./.github/workflows/deploy.yml
    with:
      environment: '1prod'