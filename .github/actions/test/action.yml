name: 'Test'
description: 'Unit tests'
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        set -euo pipefail
        echo "[INFO] [TEST] Starting unit test suite"
        echo "[INFO] [TEST] Executing test cases"
        echo "[INFO] [TEST] Unit test suite completed status=PASSED"