name: 'E2E Test'
description: 'End-to-end user scenario tests'
inputs:
  base-url:
    description: 'Deployed app URL'
    required: true
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        echo "[INFO] [E2E] Running scenarios against ${{ inputs.base-url }}"
        echo "[INFO] [E2E] PASSED"
