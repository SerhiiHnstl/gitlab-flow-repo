name: 'Performance Test'
description: 'Load and performance tests'
inputs:
  base-url:
    description: 'Deployed app URL'
    required: true
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        echo "[INFO] [PERF] Load test against ${{ inputs.base-url }}"
        echo "[INFO] [PERF] PASSED"
