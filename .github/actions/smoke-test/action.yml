name: 'Smoke Test'
description: 'Basic post-deploy health checks'
inputs:
  base-url:
    description: 'Deployed app URL'
    required: true
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        echo "[INFO] [SMOKE] Checking ${{ inputs.base-url }}/health"
        echo "[INFO] [SMOKE] PASSED"
