name: 'Smoke Test'
description: 'Basic post-deploy health checks'
runs:
  using: 'composite'
  steps:
    - shell: bash
      run: |
        set -euo pipefail
        echo "[INFO] [SMOKE] Starting smoke tests"
        echo "[INFO] [SMOKE] Verifying service health endpoint"
        echo "[INFO] [SMOKE] Smoke tests completed status=PASSED"